version: '3.1'
services:
  haproxy:
    image: haproxy
    container_name: haproxy
#    restart: always
    volumes:
      - ./conf:/usr/local/etc/haproxy
    privileged: true
    expose:
      - 80
    ports:
      - 80:80